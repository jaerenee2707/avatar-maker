/*
 <div className="item-display">
                    {selectedCategory && selectedSubCategory && selectedSubCategory.choices.map((choice) => (
                        <ItemButton key={choice.name} {...choice} handleChoiceSelect={handleChoiceSelect} />
                    ))} 
                </div> */